<Page x:Class="Skill_level_assessment_for_MCK_KTITS.SubPages.DisctrictsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Skill_level_assessment_for_MCK_KTITS.SubPages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="DisctrictsPage">
    <Page.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="Height" Value="30"/>
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="91*"/>
            <RowDefinition Height="358*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1">
            <ScrollViewer Grid.Row="1" Height="248">
                <DataGrid x:Name="dgDisctricts" AutoGenerateColumns="True" AutoGeneratingColumn="dgDisctricts_AutoGeneratingColumn"/>
            </ScrollViewer>
            <Label x:Name="districtsCount" Content="Заглушка"></Label>
        </StackPanel>
        <StackPanel Grid.Row="0" Orientation="Horizontal" FlowDirection="LeftToRight" Margin="20" HorizontalAlignment="Center">
            <Button Content="➕" Margin="10 0" x:Name="btnAdd" Click="btnAdd_Click"/>
            <Button Content="✖" x:Name="btnDelete" Click="btnDelete_Click"/>
            <Button Content="✔" Margin="10 0" x:Name="btnApply" Click="btnApply_Click"/>
        </StackPanel>
    </Grid>
</Page>
